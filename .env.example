# Firebase Admin SDK Service Account Key
# Required for server-side Firebase operations (Admin SDK)
# This should be the full JSON service account key as a string
# Get this from Firebase Console > Project Settings > Service Accounts > Generate New Private Key
FIREBASE_SERVICE_ACCOUNT_KEY='{"type":"service_account","project_id":"your-project-id",...}'

# Stripe Secret Key
# Required for payment processing via Stripe API
# Get this from Stripe Dashboard > Developers > API Keys
STRIPE_SECRET_KEY=sk_test_...

# Feature Flags
# Set to 'true' to use real AI predictions, 'false' for mock data (default: false)
# Note: Real AI predictions will incur costs via Google Gemini API
USE_REAL_AI_PREDICTIONS=false

# Optional: Google Gemini API Configuration
# If using real AI predictions, ensure your Google Cloud project has the Gemini API enabled
# GOOGLE_CLOUD_PROJECT=your-project-id

# Firebase Client Configuration (Public - already in src/firebase/config.ts)
# These values are public and safe to commit to version control
# Included here for reference during multi-environment deployments
# NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
# NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id
# NEXT_PUBLIC_FIREBASE_API_KEY=your-api-key
# NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
# NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-sender-id

# Development
# NODE_ENV=development

# Production
# NODE_ENV=production
